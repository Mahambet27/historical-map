# Download manifests

Each external download must record source ID, source version, canonical URL,
retrieval time, checksum, license decision, filter scope, and importer version.
The pipeline does not scrape unstable pages or bypass authentication/rate limits.
